FROM node:22-alpine

WORKDIR /app

# Copy only package files first for caching
COPY package*.json ./
RUN npm ci --omit=dev

# Copy the rest of backend source
COPY . .

EXPOSE 3100

CMD ["node", "server.js"]
